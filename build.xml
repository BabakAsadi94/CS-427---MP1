<?xml version="1.0" encoding="UTF-8" standalone="no"?>
<project basedir="." default="test" name="basadi2">

    <!-- Properties -->
    <property environment="env"/>
    <property name="junit.output.dir" value="ant-target/reports"/>
    <property name="debuglevel" value="source,lines,vars"/>
    <property name="target" value="1.8"/>
    <property name="source" value="1.8"/>

    <!-- Path declaration including all jar files in "ant-libs" directory -->
    <path id="basadi2.classpath">
        <fileset dir="ant-libs" includes="*.jar"/>
        <pathelement location="ant-target/classes"/>
    </path>

    <!-- Clean target -->
    <target name="clean">
        <delete dir="ant-target/test-classes"/>
        <delete dir="ant-target/classes"/>
        <delete dir="${junit.output.dir}"/>
    </target>

    <!-- Build target: Compile source Java files -->
    <target name="build">
        <mkdir dir="ant-target/classes"/>
        <javac debug="true" debuglevel="${debuglevel}" destdir="ant-target/classes"
               includeantruntime="false" source="${source}" target="${target}" encoding="UTF-8">
            <src path="src/main/java"/>
            <classpath refid="basadi2.classpath"/>
        </javac>
    </target>

    <!-- Build-test target: Compile test Java files and copy resources -->
    <target name="build-test" depends="build">
        <mkdir dir="ant-target/test-classes"/>
        <javac debug="true" debuglevel="${debuglevel}" destdir="ant-target/test-classes"
               includeantruntime="false" source="${source}" target="${target}" encoding="UTF-8">
            <src path="src/test/java"/>
            <classpath refid="basadi2.classpath"/>
        </javac>
        <copy todir="ant-target/test-classes">
            <fileset dir="src/test/resources"/>
        </copy>
    </target>

    <!-- Generate Javadoc Documentation -->
    <target name="javadoc" depends="build">
        <mkdir dir="ant-target/reports/html"/>
        <javadoc destdir="ant-target/reports/html" sourcepath="src/main/java"
                 encoding="UTF-8" classpathref="basadi2.classpath">
            <doctitle>jsoup API Documentation</doctitle>
            <use/>
            <author/>
            <version/>
        </javadoc>
    </target>

    <!-- Test target: Execute tests and generate reports -->
    <target name="test" depends="build-test,javadoc">
        <mkdir dir="${junit.output.dir}"/>
        <mkdir dir="${junit.output.dir}/html"/>

        <junit fork="yes" printsummary="withOutAndErr">
            <formatter type="xml"/>
            <batchtest todir="${junit.output.dir}">
                <fileset dir="ant-target/test-classes">
                    <include name="**/*Test.class"/>
                </fileset>
            </batchtest>
            <classpath>
                <path refid="basadi2.classpath"/>
                <pathelement location="ant-target/test-classes"/>
            </classpath>
        </junit>

        <junitreport>
            <fileset dir="${junit.output.dir}">
                <include name="TEST-*.xml"/>
            </fileset>
            <report format="frames" todir="${junit.output.dir}/html"/>
        </junitreport>
    </target>

</project>
